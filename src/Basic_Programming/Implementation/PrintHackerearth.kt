package Basic_Programming.Implementation

/*
Basic_Programming.Implementation
Standley Eugene
9/23/2017
Work in Progress
*/
fun main(args: Array<String>){
    var n = readLine()!!.toInt()
    var Str = readLine()!!.toString()
    var t = "hackerearth"
    var done = false

    while(done) {
        for (a in 0..(t.length - 1)) {
            //if(Str.find { it -> true })
        }

        if(Str.isEmpty())
            done = true
    }
}